# Quant System — AI Architecture Rules

**Status: MANDATORY / HIGHEST-PRIORITY PROJECT RULE**

本文件是本项目所有 AI Agent、开发人员及自动化工具必须遵守的架构规则。任何与本项目相关的开发、修改、重构、分析和新增功能，都必须先遵守本文件。

## 1. 核心铁律：Skill 是逻辑层，Python 是执行层

Skill 负责业务逻辑、分析逻辑、判断逻辑、交易逻辑、评分模型、分类与分级、条件判断、信号判断、财报分析、预期差分析、因果归因、策略规则、风险判断、买入 / 卖出 / 持有 / 观察等结论、所有具有业务含义的规则、所有需要“为什么这样判断”的逻辑。

Python 负责数据获取、API 调用、数据清洗、数据转换、数据格式化、调度、Skill 调用、数据库读写、结果存储、历史数据管理、服务接口、前端数据供应、展示。Python 不得承担核心业务判断。

## 2. Python 不得复制 Skill 逻辑

禁止为了方便、性能或开发速度，在 Python 中重新实现 Skill 已经定义的逻辑。如果 20% 和 +10分 是业务规则，这属于 Skill，而不是 Python。

正确架构：Python → 获取数据 → 调用 Skill → Skill 进行分析、判断、评分 → 返回结构化结果 → Python 保存结果 → 数据库 / API / 前端。

## 3. GitHub Skill 是业务逻辑的权威来源

GitHub 中维护的 Skill / Knowledge 是本项目业务逻辑的 Single Source of Truth（唯一权威来源）。如果 Python、数据库、文档或其他地方与 Skill 中的业务规则发生冲突：以 Skill 为准。不得为了让 Python 当前代码“跑起来”，反过来修改或绕过 Skill 的逻辑。

## 4. 数据库不是业务逻辑层

数据库主要用于保存原始数据、处理后的数据、Skill 输入、Skill 输出、历史结果、时间序列、状态、运行记录、审计记录。数据库中的数值不能被 Python 擅自解释成业务规则。“80 分代表什么等级、是否应该买入、为什么是 80”，必须由 Skill 定义。

## 5. 修改逻辑时必须修改 Skill

用户提出修改评分标准时：用户需求 → 确认对应 Skill → 修改 Skill → 测试 Skill → Python 继续调用 Skill → 保存新的结果。禁止直接修改 Python 判断条件。

## 6. 新功能开发顺序

① 明确业务逻辑；② 确认 / 新建 Skill；③ 将逻辑写入 Skill；④ 定义 Skill 输入与输出；⑤ 测试 Skill；⑥ Python 接入 Skill；⑦ Python 负责数据流转；⑧ 数据库存储结果；⑨ API / 前端展示。不得反过来先写 Python，再把逻辑硬塞进去。

## 7. AI 开始工作前必须检查本文件

任何 AI Agent 在开始修改本项目之前，都必须：读取 AI_RULES.md；确认当前任务属于“逻辑层”还是“执行层”；如果涉及业务判断，优先检查相关 Skill；禁止直接把业务逻辑写入 Python；完成修改后再次检查是否违反本文件。如果无法确认某段代码属于逻辑层还是执行层：默认按照逻辑层处理，优先放入 Skill。

## 8. 防止逻辑漂移

禁止出现 Skill 一套规则、Python 一套规则、数据库又保存一套规则、前端再判断一套规则。这种架构属于逻辑漂移（Logic Drift），禁止出现。业务规则必须尽可能保持：Skill → 唯一业务判断 → 结构化输出 → Python 执行 → 数据库 / API / UI。

## 9. Python 可以做“技术判断”，不能做“业务判断”

允许 Python 判断 API 是否返回成功、字段是否存在、数据格式是否正确、数据库连接是否成功、任务是否执行成功、文件是否存在、请求是否超时。不允许 Python 判断股票是否值得买、公司基本面是否优秀、财报是否超预期、评分应该是多少、风险等级是什么、信号是否成立、应该买还是卖、为什么产生这个交易信号。后者必须交给 Skill。

## 10. Skill 输出必须结构化

Skill 应尽可能输出结构化结果，例如：

```json
{
  "decision": "...",
  "score": 0,
  "level": "...",
  "reason": "...",
  "risk": "...",
  "confidence": 0
}
```

Python 不负责重新解释这些字段的业务含义。Python 的职责是：接收 → 保存 → 查询 → 展示，而不是接收 → 重新判断 → 修改 → 保存。

## 11. 发生冲突时的优先级

1. 用户明确的新要求
2. AI_RULES.md
3. GitHub Skill / Knowledge
4. 项目技术实现
5. Python 代码中的旧逻辑
6. 数据库中的历史逻辑

旧 Python 代码绝不能因为“以前就是这么写的”而成为业务规则依据。

## 12. 核心原则

Skill 决定“怎么想、怎么判断、得出什么结论”。Python 决定“怎么取、怎么跑、怎么存、怎么传、怎么展示”。

**Skill 是大脑，Python 是手脚。** Python 可以执行 Skill 的决定，但不能替 Skill 做决定。

## 13. 最终检查

每次修改代码后，AI 必须自检：是否新增了业务判断？如果新增了，是否放进 Skill？Python 是否出现新的业务规则？是否复制了 Skill 中已有的判断？是否修改了 Skill 输出的业务含义？是否出现 Skill 与 Python 两套规则？是否应该新增或修改 Skill？是否遵守本 AI_RULES.md？

如果任何一项违反：必须先修正架构，再提交代码。

END OF AI_RULES.md
